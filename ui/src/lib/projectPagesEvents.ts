/** Cross-route project pages UI events (sub-header → list page coordination). */

/** Fired by the project sub-header when the user clicks the "Add page" button. */
export const PROJECT_PAGES_CREATE_EVENT = 'project-pages-create';

/** Fired when the list should refetch (post-create from elsewhere, etc.). */
export const PROJECT_PAGES_REFRESH_EVENT = 'project-pages-refresh';
